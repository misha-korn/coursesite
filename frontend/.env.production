# Пусто = запросы идут относительными путями (/api/...), то есть на тот же
# домен, с которого открыта страница. В проде фронт и API за одним nginx,
# поэтому отдельный адрес не нужен и CORS не возникает.
VITE_API_URL=
